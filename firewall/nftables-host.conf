#!/usr/sbin/nft -f

flush ruleset

table inet filter {
    # Tailscale CGNAT range
    set tailscale-nets {
        type ipv4_addr
        flags interval
        elements = { 100.64.0.0/10 }
    }

    chain input {
        # Fixed: Shifted to standard filter priority to avoid racing with bridge drivers
        type filter hook input priority filter; policy drop;

        meta nfproto ipv6 drop
        ct state invalid drop
        iif lo accept

        # Allow DHCP requests from Incus containers
        iifname "incusbr0" udp dport 67 accept

        # Allow DNS queries from Incus containers
        iifname "incusbr0" tcp dport 53 accept
        iifname "incusbr0" udp dport 53 accept

        # Anti-spoofing
        ip saddr @tailscale-nets iifname != "tailscale0" drop

        # Allow established/related connections
        ct state established,related accept

        # --- FIX: ALLOW INBOUND HOLE PUNCHING PAYLOADS ---
        # Allows unsolicited UDP traversal return packets to reach the Incus network stack
        iifname "eth0" udp dport 32768-61000 accept

        # SSH from Tailscale (rate limited)
        iifname "tailscale0" ip saddr @tailscale-nets tcp dport 22 ct state new limit rate 5/minute burst 5 packets accept

        # Incus WebUI from Tailscale (rate limited)
        iifname "tailscale0" ip saddr @tailscale-nets tcp dport 8443 ct state new limit rate 20/second burst 40 packets accept

        # ICMP from Tailscale (rate limited)
        iifname "tailscale0" ip saddr @tailscale-nets icmp type echo-request limit rate 5/second accept

        # Drop everything else from Tailscale (explicitly log)
        iifname "tailscale0" ip saddr @tailscale-nets limit rate 3/minute burst 5 packets log prefix "nft-ts-unauthorized: " drop
        iifname "tailscale0" ip saddr @tailscale-nets drop

        # Log all other dropped packets (rate limited)
        limit rate 3/minute burst 5 packets log prefix "nft-input-drop: " drop
        drop
    }

    chain forward {
        type filter hook forward priority filter; policy drop;

        meta nfproto ipv6 drop
        ct state invalid drop

        # Anti-spoofing for forwarded traffic
        ip saddr @tailscale-nets iifname != "tailscale0" drop
        ip daddr @tailscale-nets oifname != "tailscale0" drop

        # Allow Incus containers outbound internet access
        iifname "incusbr0" oifname "eth0" accept

        # Global connection tracking allowance for forwarded frames
        ct state established,related accept

        # Block Tailscale -> Internet forwarding (Anti-Exit Node Abuse)
        iifname "tailscale0" oifname "eth0" log prefix "nft-exit-blocked: " drop

        limit rate 3/minute burst 5 packets log prefix "nft-forward-drop: " drop
        drop
    }

    chain output {
        type filter hook output priority filter; policy accept;
        meta nfproto ipv6 drop
    }
}

# Unified Table Fix for Dynamic Subnets
table inet nat {
    chain postrouting {
        type nat hook postrouting priority srcnat; policy accept;

        # FIX: Matches using the source network address range instead of invalid iifname
        ip saddr 10.0.0.0/24 oifname "eth0" masquerade
    }
}